import React from "react";

const MovieListHeading = (props) => {
    return (
        <div className="col">
            <hi>{props.heading}</hi>
        </div>
    );
};

export default MovieListHeading;